<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 880 150" width="100%" height="150" fill="none">
  <defs>
    <!-- Organic filter to simulate hand-carved seal edge on washi paper -->
    <filter id="hanko-roughness-light" x="-10%" y="-10%" width="120%" height="120%">
      <feTurbulence type="fractalNoise" baseFrequency="0.045" numOctaves="3" result="noise" />
      <feDisplacementMap in="SourceGraphic" in2="noise" scale="2" xChannelSelector="R" yChannelSelector="G" />
    </filter>
  </defs>

  <style>
    .name-light {
      font-family: 'Cinzel', 'Playfair Display', 'Times New Roman', 'Hiragino Mincho ProN', Georgia, serif;
      font-size: 26px;
      font-weight: 500;
      letter-spacing: 6px;
      fill: #1C1B19;
    }
    .katakana-light {
      font-family: 'Hiragino Mincho ProN', 'Yu Mincho', 'Noto Serif JP', serif;
      font-size: 11px;
      letter-spacing: 5px;
      fill: #8A857C;
    }
    .sub-light {
      font-family: ui-monospace, 'SF Mono', 'Cascadia Code', Menlo, Consolas, monospace;
      font-size: 12px;
      letter-spacing: 2px;
      fill: #8A857C;
    }
    .status-light {
      font-family: ui-monospace, 'SF Mono', 'Cascadia Code', Menlo, Consolas, monospace;
      font-size: 11px;
      letter-spacing: 1px;
      fill: #A8A297;
    }
    .stamp-text-light {
      font-family: 'Hiragino Mincho ProN', 'Yu Mincho', 'Noto Serif JP', 'HiraMinProN-W6', serif;
      font-weight: 700;
      fill: #C8382B;
    }
  </style>

  <!-- Left vertical Katakana accent -->
  <text x="36" y="38" class="katakana-light">プラチャクシュ・トマール</text>

  <!-- Vertical hairline divider between Katakana and English name -->
  <line x1="230" y1="24" x2="230" y2="126" stroke="#D9D3C7" stroke-width="1" />

  <!-- Main identity block -->
  <g transform="translate(254, 0)">
    <!-- Name -->
    <text x="0" y="52" class="name-light">PRATYAKSH TOMAR</text>

    <!-- Subtitle / Focus area -->
    <text x="2" y="82" class="sub-light">AUTONOMOUS AGENTS  ·  SYSTEM RUNTIMES  ·  APPLIED ML</text>

    <!-- Quiet telemetry line -->
    <g transform="translate(2, 114)">
      <!-- Vermilion micro-pulse dot -->
      <circle cx="4" cy="-4" r="3" fill="#C8382B" opacity="0.85">
        <animate attributeName="opacity" values="0.85;0.2;0.85" dur="3.6s" repeatCount="indefinite" />
      </circle>
      <text x="18" y="0" class="status-light">midnight buffer  ·  chai going cold  ·  ak-tu / new delhi</text>
    </g>
  </g>

  <!-- Hand-carved Vermilion Hanko Stamp (1% accent) -->
  <g transform="translate(800, 38)" filter="url(#hanko-roughness-light)">
    <!-- Outer stamp boundary: hand-pressed square with rounded corners -->
    <rect x="0" y="0" width="48" height="48" rx="8" fill="none" stroke="#C8382B" stroke-width="2.5" opacity="0.9" />
    <rect x="3.5" y="3.5" width="41" height="41" rx="5" fill="none" stroke="#C8382B" stroke-width="0.8" opacity="0.5" />
    <!-- Traditional seal character: 拓 (Taku / Hiraku: To pioneer, craft, break new ground) -->
    <text x="24" y="33" text-anchor="middle" font-size="24" class="stamp-text-light">拓</text>
  </g>
</svg>
